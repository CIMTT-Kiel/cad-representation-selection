[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "clearshape"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "numpy==2.2.2",
    "pandas",
    "omegaconf",
    "scikit-learn",
    "cadquery",
    "steputils",
]
